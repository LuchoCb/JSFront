let usuario = "";
let password = "";


        while (usuario != "CoderStudent") {
            alert ("usuario incorrecto");
            usuario = prompt ("Digite nombre de usuario");
        }
        if (usuario === "CoderStudent") {
            password = prompt ("digite su contraseña")
        }
        while (password != "Javascript") {
            alert ("contraseña incorrecta");
            password = prompt ("digite su contraseña");
        }
        if (password === "Javascript") {
            alert ("BIENVENIDO");
        }
                
        
        

        
    